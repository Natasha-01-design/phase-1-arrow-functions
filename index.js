const saturdayFun = (role= "roller-skate") => `This Saturday, I want to ${role}!`;
const mondayWork = (office = "go to the office") => `This Monday, I will ${office}.`;
const wrapAdjective = (visualFlair = "*") => {
  return (adjective = "special") => {
    return `You are ${visualFlair}${adjective}${visualFlair}!`;
  };
};
const divide = function() {
  return 2000 / 100;
};
const square = n => n * n;
const add = (c, d) => c + d;
